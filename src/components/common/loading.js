import React from 'react';

const Loading = () => (
    (<div className="text-center centerOfScreen">
        <p className="loading">Loading<span>.</span><span>.</span><span>.</span></p>
    </div>
)
);

export default Loading;